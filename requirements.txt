qdrant-client>=1.7.0
sentence-transformers
# Optional: pip install sentence-transformers[onnx] for 2-3x faster embeddings
ollama
nltk>=3.8.0

# Web UI
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Optional dependencies for extended file support
# OCR: pytesseract (default) + Pillow. For AI OCR (faster on GPU):
#   pip install paddlepaddle paddleocr   # PaddleOCR (~12 fps on GPU)
#   pip install easyocr                 # EasyOCR (~4 fps on GPU)
PyPDF2>=3.0.0
python-docx>=0.8.11
Pillow>=9.0.0
pytesseract>=0.3.10
PyMuPDF>=1.23.0
pypdfium2>=4.0.0
pdfminer.six>=20220524
beautifulsoup4>=4.11.0
lxml>=4.9.0
psutil>=5.9.0
